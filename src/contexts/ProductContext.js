import { createContext } from "react";

export const ProductContext = createContext();

// export default ProductContext;
//this is what allows us to prop drill our product context from anything and anywhere.